// it returns elements of position of element index
// if no argument are matching thn it will return -1

// syntax
// array.indexOf(item, start)
const fruits = ["Banana", "Orange", "Apple", "Mango", "Apple"];

console.log(fruits.indexOf("Apple", 0))