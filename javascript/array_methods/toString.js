// it is change array of data in to string
// it won't change the original array

let arr = ["bino", "sameer", "linkan", "sujit"];

console.log(arr, typeof arr);
let arr1 = arr.toString()
console.log(arr1, typeof arr1);