// length

// it will give the length of the sting and length of empty is 0

let str = "binod";  // it will give you a length of 5
let str1 =""  // empty string will give you a value of 0 only
let str2 =["binod", "sameer", "sujit"]  // array of data
console.log(str.length);   // 5
console.log(str1.length);  // 0
console.log(str2.length);  // 3